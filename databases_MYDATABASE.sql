create or replace database MYDATABASE;

create or replace schema PUBLIC;

create or replace TABLE EVENTS (
	DEVICE_TYPE VARCHAR(16777216),
	VERSION VARCHAR(16777216),
	F NUMBER(38,0),
	RV VARIANT,
	T NUMBER(38,0),
	ACHZ NUMBER(38,0),
	ACV NUMBER(38,0),
	DCA NUMBER(38,0),
	DCV NUMBER(38,0),
	ENJR NUMBER(38,0),
	ERRS NUMBER(38,0),
	MXEC NUMBER(38,0),
	TMPI NUMBER(38,0),
	VD NUMBER(38,0),
	Z NUMBER(38,0),
	constraint PK_DEVICETYPE primary key (DEVICE_TYPE, RV)
);
create or replace TABLE FLATTENED_SOURCE (
	DEVICE_TYPE VARCHAR(16777216),
	VERSION VARCHAR(16777216),
	SRC VARIANT
);
create or replace TABLE RAW_SOURCE (
	SRC VARIANT
);